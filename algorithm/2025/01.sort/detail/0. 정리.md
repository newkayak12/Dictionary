1. 버블
	1. 인접 두 개씩 비교
	2. 한 바퀴 돌면 최대 값이 맨 뒤
	3. 회전 당 맨 뒤 인덱스는 하나씩 배제
2. 선택
	1. 현재 인덱스까지 포함해서 앞쪽을 정렬한다.
	2. 해당 구간 내 최소 값을 찾고 그 값은 현재 인덱스와 스왑
	3. 회전 당 맨 앞 인덱스 하나씩 배제
3. 삽입
	1. 현재 원소 앞에 있는 값 중 현재 원소보다 크다면 하나씩 민다
	2. 현재 원소보다 작은 수가 나올 때까지 밀고 이전 인덱스에 삽입
4. 셸
	1. Gap 산정 (보통 array / 2로 시작)
	2. Gap 간 요소 