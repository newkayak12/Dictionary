## Factorial Number System이란?
➡️ 자리 값이 factorial인 진법

```
 (1*1!) + (2*2!) + (3*3!) + ... + (k*k!) = (k+1)! - 1
```


## 순열과의 연결
```
(n-1)! 자리 : 첫 자리에서 n개 중 하나 선택
(n-2)! 자리 : 첫 자리에서 n-1개 중 하나 선택

1! 자리 : 마지막 자리에서 2개 중 하나 선택
```