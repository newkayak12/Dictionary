{
  "name": "front",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:js": "babel dist -d build",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "@types/sockjs": "^0.3.36",
    "@types/sockjs-client": "^1.5.4",
    "@types/stompjs": "^2.3.9",
    "typescript": "^5.4.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.24.1",
    "@babel/core": "^7.24.4"
  }
}
